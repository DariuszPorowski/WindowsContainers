version: '2'

services:
 web:
  build: ./web
  ports:
   - "80:80"
  links:
   - db
  tty:
    true
 db:
  build: ./db
  expose:
   - "1433"
  tty:
    true

networks:
 default:
  external:
   name: "nat"